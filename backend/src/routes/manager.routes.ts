import { Router } from 'express';

const router = Router();

// TODO: Implementar controladores de managers
router.get('/', (req, res) => {
  res.json({ message: 'Managers - Pendiente de implementar' });
});

export default router;









