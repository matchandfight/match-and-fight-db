import { Router } from 'express';

const router = Router();

// TODO: Implementar controladores de ofertas
router.get('/', (req, res) => {
  res.json({ message: 'Ofertas - Pendiente de implementar' });
});

export default router;









